# =============================================================================
# Portfolio Frontend — Environment Variables Example
# Copy this file to .env.local and fill in the values.
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# =============================================================================

# ----------------------------------------------------------------------------
# App
# ----------------------------------------------------------------------------
# Base URL of the deployed site (used for Open Graph, sitemap, canonical URLs)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# ----------------------------------------------------------------------------
# Backend API
# ----------------------------------------------------------------------------
# Server-side URL used by Next.js SSR/API routes to call the Rust backend.
# Never exposed to the browser.
BACKEND_URL=http://localhost:8080

# Public backend API URL accessible from the browser
NEXT_PUBLIC_API_URL=http://localhost:8080

# ----------------------------------------------------------------------------
# CORS
# ----------------------------------------------------------------------------
# Comma-separated list of allowed origins for the Next.js middleware CORS check
ALLOWED_ORIGINS=http://localhost:3000

# ----------------------------------------------------------------------------
# Logging
# ----------------------------------------------------------------------------
# Log ingestion endpoint (defaults to the built-in /api/logs route)
NEXT_PUBLIC_LOG_API_URL=/api/logs

# Minimum log level sent from the browser: trace | debug | info | warn | error
NEXT_PUBLIC_LOG_LEVEL=info

# ----------------------------------------------------------------------------
# Encryption / Crypto
# ----------------------------------------------------------------------------
# Session TTL in milliseconds (default: 900000 = 15 minutes)
CRYPTO_SESSION_TTL=1

# PBKDF2-SHA-256 iteration count (default: 100000).
# Higher = stronger against brute-force but slower handshake.
# Server sends this value to the client during ECDH handshake — client never hardcodes it.
CRYPTO_PBKDF2_ITERATIONS=1

# ----------------------------------------------------------------------------
# Roadmap.sh Integration (optional)
# ----------------------------------------------------------------------------
# All roadmap data is fetched via the backend — use the backend API routes:
#   GET /api/roadmap/streak
#   GET /api/roadmap/dashboard
#   GET /api/roadmap/teams
#   GET /api/roadmap/favourites
# Set ROADMAP_AUTH_TOKEN in portfolio-backend/.env instead.

# ----------------------------------------------------------------------------
# GitHub Integration (optional)
# ----------------------------------------------------------------------------
# Personal Access Token used server-side to raise the GitHub API rate limit
GH_TOKEN=

# GitHub username for fetching repositories / profile data
GH_USERNAME=exampleuser
