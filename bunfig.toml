preload = ["./src/test/bun-setup.ts"]
timeout = 10000
# Exclude test files that cannot run under Bun's native test runner:
# - e2e/ uses Playwright (needs playwright test runner)
# - layout.integration.test fails due to next/font/google ESM-only exports
exclude = ["e2e/**", "src/app/test/layout.integration.test.tsx"]
