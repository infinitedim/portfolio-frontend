# Feature #31 — Full CLI (pipe, chains)

**Status:** Done

## Frontend

- `command-parser.ts`: `|` pipe, `;` and `&&` chains
- Piped stdout passed as args to next stage

## Backend

- Optional overlap with analytics counters only
