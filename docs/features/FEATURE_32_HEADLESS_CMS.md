# Feature #32 — Headless CMS Mode

**Status:** Done

## Backend

- `HEADLESS_CMS_ENABLED`, `api_keys` table, `X-Api-Key` middleware
- `GET /api/v1/content/blog`, `/portfolio`; admin-scope PATCH

## Frontend

- `/admin/cms` — setup docs, endpoint reference, SQL key template
